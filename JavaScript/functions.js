// Functions
const sum = function (num1, num2) {
  return num1 + num2;
};

// const results = sum(20, 33);
// console.log("Result: ", results);

// Arrow function
let multiply = (n1, n2) => n1 * n2;

// const u = multiply(10, 5);
// console.log("Result : ", u);

// Create a function that takes a number and return percentage of that number
const percentage = (marks) => (marks / 1200) * 100;
// console.log(percentage(1000));
